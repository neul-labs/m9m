name: Release

on:
  push:
    tags:
      - 'v*.*.*'

env:
  GO_VERSION: '1.24'

permissions:
  contents: write
  packages: write
  id-token: write
  pull-requests: read

jobs:
  get-version:
    name: Get Version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.version }}
    steps:
      - name: Get version from tag
        id: get_version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

  build-release:
    name: Build Release Binaries
    needs: get-version
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            artifact_name: m9m-linux-amd64
          - os: ubuntu-latest
            goos: linux
            goarch: arm64
            artifact_name: m9m-linux-arm64
          - os: macos-latest
            goos: darwin
            goarch: amd64
            artifact_name: m9m-darwin-amd64
          - os: macos-latest
            goos: darwin
            goarch: arm64
            artifact_name: m9m-darwin-arm64
          - os: windows-latest
            goos: windows
            goarch: amd64
            artifact_name: m9m-windows-amd64.exe

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          COMMIT="${GITHUB_SHA::7}"
          BUILD_DATE="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"
          go build -trimpath -ldflags="-w -s -X main.Version=${{ needs.get-version.outputs.version }} -X main.Commit=${COMMIT} -X main.BuildDate=${BUILD_DATE}" -o ${{ matrix.artifact_name }} ./cmd/m9m
        shell: bash

      - name: Generate checksum
        run: |
          if [ "${{ matrix.goos }}" = "windows" ]; then
            sha256sum ${{ matrix.artifact_name }} > ${{ matrix.artifact_name }}.sha256
          else
            shasum -a 256 ${{ matrix.artifact_name }} > ${{ matrix.artifact_name }}.sha256
          fi
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            ${{ matrix.artifact_name }}
            ${{ matrix.artifact_name }}.sha256

  release:
    name: Create Release
    needs: [get-version, build-release]
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate changelog
        id: changelog
        uses: metcalfc/changelog-generator@v4.3.1
        with:
          myToken: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        id: create_release
        run: |
          VERSION="${{ needs.get-version.outputs.version }}"
          TAG="${{ github.ref_name }}"

          cat > release_notes.md << 'EOF'
          ## Changes in this release
          ${{ steps.changelog.outputs.changelog || 'See the full commit history for changes in this release.' }}

          ## Installation

          ### Docker (GHCR)
          ```bash
          docker pull ghcr.io/neul-labs/m9m:${{ needs.get-version.outputs.version }}
          ```

          ### Binary
          Download the binary for your platform below.

          ### npm
          ```bash
          npm install m9m-cli@${{ needs.get-version.outputs.version }}
          ```

          ### PyPI
          ```bash
          pip install m9m-cli==${{ needs.get-version.outputs.version }}
          ```

          ### From Source
          ```bash
          git clone https://github.com/neul-labs/m9m.git
          cd m9m
          git checkout v${{ needs.get-version.outputs.version }}
          make build
          ```

          ## Checksums
          See the `.sha256` files attached to this release.
          EOF

          gh release create "$TAG" \
            --title "Release $TAG" \
            --notes-file release_notes.md \
            artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docker-release:
    name: Build and Push Docker Release
    needs: get-version
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            VERSION=${{ needs.get-version.outputs.version }}

  npm-publish:
    name: Publish to npm
    needs: get-version
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Sync version and publish
        working-directory: packages/m9m-cli/nodejs
        run: |
          VERSION=${{ needs.get-version.outputs.version }}
          npm version "$VERSION" --no-git-tag-version
          npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  pypi-publish:
    name: Publish to PyPI
    needs: get-version
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build tools
        run: pip install build hatchling

      - name: Sync version and build
        working-directory: packages/m9m-cli/python
        run: |
          VERSION=${{ needs.get-version.outputs.version }}
          sed -i "s/version = \".*\"/version = \"$VERSION\"/" pyproject.toml
          python -m build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/m9m-cli/python/dist
